

## Plan: Admin card spans full width on desktop

**File:** `src/components/trip/TripDashboard.tsx`

Add `md:col-span-2` to the last `SectionCard` (Admin) so it stretches across both columns in the 2-column desktop grid, avoiding an orphaned single card.

